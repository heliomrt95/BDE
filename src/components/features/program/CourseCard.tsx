// Feature component: single course/workshop card for the program page

import { Course } from '@/types/program';

interface CourseCardProps {
  course: Course;
}

export default function CourseCard({ course }: CourseCardProps) {
  return <article />;
}
